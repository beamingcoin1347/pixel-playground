{
  "name": "pixel-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pixel Playground - a six-game minigame arcade with an authoritative Node/Express engine.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install && npm --prefix e2e install",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix client run build",
    "start": "node scripts/start.mjs",
    "start:e2e": "node scripts/start-e2e.mjs",
    "test": "npm --prefix server test",
    "test:e2e": "npm run build && npm --prefix e2e run test",
    "test:all": "npm test && npm run test:e2e"
  }
}
